// My approach.
// if the left matches right simply incerement, don't push onto stack.
// when you know all charcs are seen and stack is non-empty, pop char by char.
// If there is no match then return false.

// If all chars can be matched properly and stack is empty, return true

class Solution {
    public boolean checkValidString(String s) {
        
    }
}
